# Flume simple agent configuration

Check the `flume_agent1.properties` for a simple agent configuration. It has a sequence generator as a source, uses a memory channel and sinks to a logger sink.

Run the `flume_agent1.sh` to start the execution of the agent described in the above file.
